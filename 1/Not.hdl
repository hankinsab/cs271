CHIP Not {
  IN in;
  OUT out;

  PARTS:
  //left side reference the chip we are trying to use 
  //wire input to both a and b
  //output of nand chip is wired to the output on the out of the not chip 
    Nand(a= in , b= in , out= out );
}